# Phase 50.4: Canonical MCP Guide Convergence

## Authority

Paperclip Semantic Core and Trend usage follows:

`/Users/savitsky/CodexProjects/seo-semantic-core/docs/AI_AGENT_MCP_USAGE.md`

The remote MCP server owns raw generation, provider evidence, immutable run artifacts, review output, cost telemetry, and import payloads. Paperclip owns operational cases, lifecycle decisions, monitoring, budgets, content planning, and downstream delegation.

## Corrected Contracts

- Semantic `run_layer` uses `mode=provider`; legacy `mode=live` is not emitted by current agent contracts.
- `provider_cache_mode=read_only` is the only autonomous no-spend mode.
- `read_write`, `refresh`, and `bypass` require a separate operator-approved exact-candidate runbook.
- A server or Paperclip provider lock creates one deduplicated monitored `external_wait`; it does not cause retries, owner alerts, or fixture substitution.
- Autonomous semantic batches contain at most 10 exact `candidate_keywords`.
- Trend generation remains a separate `mode=live` workflow, while enabled, and never creates semantic imports or topics directly.
- Content-plan construction remains downstream in native Paperclip pipelines.

## Verification

- Current routine, trend-review policy, Semantic Core Validator, and competitive-intelligence agent bootstrap text use the canonical provider/cache terminology.
- Runtime plugin policy independently normalizes legacy semantic `live` to `provider` and fails closed while provider execution is disabled.
