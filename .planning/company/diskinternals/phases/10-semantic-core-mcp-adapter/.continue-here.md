---
phase: 10-semantic-core-mcp-adapter
task: branch-transfer-handoff
total_tasks: 1
status: handoff_ready
last_updated: 2026-05-06T07:01:37.781Z
company: DiskInternals
company_id: 969d66ff-d77e-4dbf-8759-1a17c2bb17c2
issue_prefix: DIS
---

<current_state>

This handoff is for moving the DiskInternals Semantic Core MCP policy work to another branch.

Repository root:
`/path/to/paper-clip`

Paperclip source tree:
`/path/to/paper-clip/local-paperclip`

DiskInternals planning root:
`/path/to/paper-clip/.planning/company/diskinternals`

Current branch at handoff time:
`codex/upstream-v2026.403.0-convergence`

Current HEAD at handoff time:
`96d553e0 Add Astrogen handoff for semantic core continuation`

Important branch-transfer note:
the current branch contains commits after the DiskInternals semantic-core policy work that are not part of this DiskInternals handoff. For a clean DiskInternals transfer, use the commit list below instead of blindly taking the current branch HEAD.

</current_state>

<completed_work>

- Semantic Core MCP canonical agent guide was reread from `/path/to/seo-semantic-core/docs/AI_AGENT_MCP_USAGE.md`.
- DiskInternals planning now includes `SEO-MCP-11`: Semantic Core layer decisions are policy-driven.
- Phase 10 roadmap/state/execution log now say agents must avoid hardcoded lexical allow/deny assumptions, configure niche terms through project inputs/entity packs/`semantic_expansion`, route high-demand conflicts to review, and inspect `decision_trace`.
- `@paperclipai/plugin-semantic-core-mcp-agent-tools` now surfaces `keyword_rows_with_decision_trace` in the `prepare-paperclip-import` competitor expansion summary.
- Plugin README/tests now document and cover `decision_trace` visibility.
- Live Paperclip contracts were updated for DiskInternals `CMO`, `CTO`, `SEO Semantic Core Strategist`, and `SEO Semantic Core Validator`.
- Local verification passed:
  - `pnpm --filter @paperclipai/plugin-semantic-core-mcp-agent-tools test`
  - `pnpm --filter @paperclipai/plugin-semantic-core-mcp-agent-tools typecheck`
  - `pnpm --filter @paperclipai/plugin-semantic-core-mcp-agent-tools build`
- Runtime deployment was completed and verified after commit `c2f175ee`:
  - `/api/health` returned `status=ok`
  - plugin loader reported `12/12 succeeded`
  - Semantic Core plugin registered `16` tools

</completed_work>

<branch_transfer_commits>

Minimum commit to transfer if the target branch already contains the earlier Semantic Core MCP adapter updates:

```text
c2f175ee feat: align semantic core decision policy
```

Recommended full Semantic Core MCP policy/config stack if the target branch does not already contain these commits:

```text
c12b641d feat: support semantic core competitor recall config
53c5da58 docs: align semantic core MCP operating rules
3ba5036a Prevent semantic core run-layer config no-op
b641db55 Sync semantic core agent guide with MCP policy updates
c2f175ee feat: align semantic core decision policy
```

Suggested cherry-pick command from the target branch:

```bash
git cherry-pick c12b641d 53c5da58 3ba5036a b641db55 c2f175ee
```

If the target branch already includes the first four commits, cherry-pick only:

```bash
git cherry-pick c2f175ee
```

Do not automatically cherry-pick later current-branch commits after `c2f175ee` unless the new branch explicitly needs them:

```text
53716ba2 Implement SEO ops Postgres schema
b809f2a2 Harden semantic core import artifact validation
ced085b8 Document semantic core review escalation guardrails
e6d7b149 Document Astrogen semantic core layer 2 status
96d553e0 Add Astrogen handoff for semantic core continuation
```

</branch_transfer_commits>

<files_to_expect_from_c2f175ee>

```text
.planning/company/diskinternals/EXECUTION_LOG.md
.planning/company/diskinternals/REQUIREMENTS.md
.planning/company/diskinternals/ROADMAP.md
.planning/company/diskinternals/STATE.md
local-paperclip/packages/plugins/plugin-semantic-core-mcp-agent-tools/README.md
local-paperclip/packages/plugins/plugin-semantic-core-mcp-agent-tools/src/worker.ts
local-paperclip/packages/plugins/plugin-semantic-core-mcp-agent-tools/tests/plugin.spec.ts
```

</files_to_expect_from_c2f175ee>

<decisions_made>

- Semantic Core MCP is generation/provenance infrastructure, not content planning or implementation routing.
- `search_volume` remains a legacy alias for geo volume; global volume must be read from `global_search_volume`.
- Production semantic-core runs should use all active layers unless explicitly marked smoke/budget-sensitive:
  - `core_product_intent`
  - `adjacent_use_case_intent`
  - `audience_need_intent`
  - `audience_interest_intent`
- Production runs should enable competitor SERP expansion with content parsing when full recall matters.
- `semantic_expansion` and `provider_cache` belong in registered project config, not `run_layer`; `run_layer` may use `provider_cache_mode`.
- Layer membership is policy-driven. No agent or adapter should hardcode site-specific lexical vetoes.
- High-demand conflicts should go to review, typically `candidate_review` with `parked_reason=high_demand_conflict`, instead of silent parked/rejected loss.
- `decision_trace` should be inspected when explaining why keywords were accepted, put in review, parked, or rejected.

</decisions_made>

<blockers>

- No current blocker for the handoff itself.
- Live agent contract updates were performed through Paperclip API and are not represented as Git files. If moving only Git commits to a different environment, repeat or verify the live contract sections there.
- Current branch HEAD includes non-DiskInternals follow-up commits after `c2f175ee`; branch transfer should choose commits intentionally.

</blockers>

<context>

The target state for DiskInternals is that agents use the Semantic Core MCP plugin through Paperclip capabilities, preserve evidence, and explain semantic-core outcomes from MCP artifacts instead of guessing. The newest important MCP behavior is policy-driven layer decisions. This means the proper place for project/niche/audience/exclusion terms is in project inputs, entity packs, and `semantic_expansion`, not in Paperclip code or agent assumptions.

When a keyword conflicts with current layer policy but has high demand evidence, the safe behavior is review escalation. The validator should reject a semantic-core delivery that silently loses high-demand candidates without enough `decision_trace` or review evidence.

</context>

<next_action>

On the target branch, first check whether the earlier Semantic Core MCP adapter commits are present:

```bash
git log --oneline -- local-paperclip/packages/plugins/plugin-semantic-core-mcp-agent-tools .planning/company/diskinternals
```

Then cherry-pick the minimum required commit set from `<branch_transfer_commits>`, run:

```bash
cd /path/to/paper-clip/local-paperclip
pnpm --filter @paperclipai/plugin-semantic-core-mcp-agent-tools test
pnpm --filter @paperclipai/plugin-semantic-core-mcp-agent-tools typecheck
pnpm --filter @paperclipai/plugin-semantic-core-mcp-agent-tools build
```

If the target branch will be deployed to Paperclip, repeat live verification:

```text
/api/health = ok
Semantic Core plugin registered 16 tools
plugin-loader loadAll complete with failed=0
```

</next_action>
