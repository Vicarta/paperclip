# Exa Agent Tools

Worker-only Paperclip plugin that exposes Exa-backed agent tools:

- web search
- page crawling
- code-context lookup

The plugin is intended for local-path installs during development. Configure the
Exa API key via a Paperclip secret reference in plugin settings.
